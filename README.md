# Mobile_Robotics_Final

## Getting Started

Please run the following command to install all required dependencies. We recommend creating and using a virtual environment to avoid package management conflicts.

```sh
python3 -m venv [name of venv]
pip install -r requirements.txt
```